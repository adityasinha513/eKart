import AdminPlaceholder from "./AdminPlaceholder";

export default function AdminCategories() {
  return <AdminPlaceholder title="Categories" description="Manage the product categories shown across the storefront." />;
}
